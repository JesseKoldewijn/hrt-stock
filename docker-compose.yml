version: "3.1"

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PORT: 5432
      POSTGRES_DB: hrt-countries
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
